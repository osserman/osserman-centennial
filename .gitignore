# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
dist/

# OS
.DS_Store

# Editors
.vscode/
.idea/

# NOTE: reports/ is deliberately committed.
# data/lineages.db is excluded from this public repo (too large for GitHub,
# and unneeded here — the viz layer reads only the precomputed JSON exports
# under viz/static/data). It stays committed in the private working copy.
data/lineages.db

# SQLite scratch files (transient; the .db itself IS committed)
*.db-journal
*.db-wal
*.db-shm
