// Every route in this app is fully static (no server-side dynamic
// behavior anywhere) — declared once here so new chapters don't need to
// repeat `export const prerender = true` in their own +page.js.
export const prerender = true;
